# SNS Profile Site

This repository contains a simple static mirror of the public profile page for the Team Mirai candidate site.

## Run locally

Open index.html in a browser, or serve the folder with a simple static server:

```bash
python -m http.server 8000
```
